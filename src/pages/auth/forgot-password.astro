---
import BaseLayout from "@/layouts/BaseLayout.astro";
import ForgotPasswordForm from "@/components/auth/ForgotPasswordForm.svelte";
import Card from "@/components/ui/Card.svelte";
---

<BaseLayout title="Lupa Password - UMKM Site Builder" description="Atur Ulang Kata Sandi Anda" hideNavbar={true}>
  <main class="min-h-screen w-full flex items-center justify-center p-4 bg-canvas relative overflow-hidden">
    <div class="w-full max-w-md relative z-10 animate-fade-in-up">
      <Card variant="elevated" padding="xl" radius="3xl">
        <div class="text-center mb-8">
          <a href="/" class="inline-flex items-center gap-2 hover:opacity-80 transition-opacity w-fit mx-auto mb-6 focus:outline-none focus-visible:ring-2 focus-visible:ring-primary/40 rounded-xl">
            <div class="w-8 h-8 rounded-xl bg-primary text-white flex items-center justify-center font-heading font-bold text-sm shadow-sm">U</div>
            <span class="text-heading-md text-main tracking-tight">UMKM Builder</span>
          </a>
          <h1 class="text-heading-lg text-main">Lupa Kata Sandi?</h1>
          <p class="text-body-base text-secondary mt-2">
            Masukkan alamat email Anda yang terdaftar dan kami akan mengirimkan tautan untuk mengatur ulang kata sandi.
          </p>
        </div>

        <ForgotPasswordForm client:load />
      </Card>
    </div>
  </main>
</BaseLayout>
