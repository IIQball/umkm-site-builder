---
import BaseLayout from "@/layouts/BaseLayout.astro";
import RegisterForm from "@/components/auth/RegisterForm.svelte";
---

<BaseLayout title="Buat Akun - UMKM Site Builder" description="Buat akun UMKM Site Builder Anda" hideNavbar={true}>
  <main class="min-h-[100dvh] flex items-center justify-center px-4 py-12 bg-canvas">
    <div class="w-full max-w-md">
      <div class="text-center mb-8">
        <h1 class="text-2xl sm:text-3xl font-extrabold tracking-tight text-main">Buat Akun</h1>
        <p class="text-sm text-secondary mt-2">Mulai bangun toko UMKM Anda hari ini</p>
      </div>
      <div class="card border border-border shadow-2xl rounded-3xl p-6 sm:p-9 bg-card backdrop-blur-sm">
        <RegisterForm client:load />
        <p class="text-center text-sm text-secondary mt-6 pt-5 border-t border-border-light">
          Sudah punya akun?
          <a href="/auth/login" class="link link-primary font-semibold ml-1">Masuk</a>
        </p>
      </div>
    </div>
  </main>
</BaseLayout>
